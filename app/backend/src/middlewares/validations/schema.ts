// import Joi = require('joi');

// const userBodyLoginSchema = Joi.object({
//   email: Joi.string().min(1).required(),
//   password: Joi.string().min(6).required(),
// });

// const matcheBodyUpdateInProgressSchema = Joi.object({
//   homeTeamId: Joi.number().min(1).required(),
//   awayTeamId: Joi.number().min(1).required(),
//   homeTeamGoals: Joi.number().min(0).required(),
//   awayTeamGoals: Joi.number().min(0).required(),
// });

// const matcheBodyUpdateSchema = Joi.object({
//   homeTeamGoals: Joi.number().min(0).required(),
//   awayTeamGoals: Joi.number().min(0).required(),

// });

// export default {
//   userBodyLoginSchema,
//   matcheBodyUpdateInProgressSchema,
//   matcheBodyUpdateSchema,
// };
